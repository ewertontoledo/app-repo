name: Build image -> Push to OCIR, Trivy Scan and Update Fleet repo

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      APP_NAME: "app-teste-2"
      APP_PORT: 9100
      TRIVY_FAIL_ON: "true"
      SHORT_SHA: ${{ github.sha }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to OCIR
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.OCIR_REGISTRY }}
          username: ${{ secrets.OCIR_USERNAME }}
          password: ${{ secrets.OCIR_AUTH_TOKEN }}

      - name: Build and push image if not exists
        run: |
          set -euo pipefail
          IMAGE="gru.ocir.io/${{ secrets.IMAGE_NAMESPACE }}/poc/image/${APP_NAME}:${SHORT_SHA}"
          echo "🔍 Verificando se a imagem \"$IMAGE\" já existe..."
          if docker manifest inspect "$IMAGE" > /dev/null 2>&1; then
            echo "✅ Imagem $IMAGE já existe"
          else
            echo "📦 Buildando e pushando $IMAGE..."
            docker buildx build --platform linux/amd64,linux/arm64 -t "$IMAGE" --push .

      - name: Scan image with Trivy
        run: |
          set -euo pipefail
          IMAGE="gru.ocir.io/${{ secrets.IMAGE_NAMESPACE }}/poc/image/${APP_NAME}:${SHORT_SHA}"
          echo "🔒 Escaneando $IMAGE com Trivy..."
          curl -sfL https://github.com/aquasecurity/trivy/releases/latest/download/trivy_0.65.0_Linux-64bit.tar.gz -o trivy.tar.gz
          tar zxvf trivy.tar.gz
          sudo mv trivy /usr/local/bin/
          mkdir -p trivy-reports
          trivy image --exit-code 0 --severity HIGH,CRITICAL --format json -o trivy-reports/trivy-report.json "$IMAGE"
          zip -r trivy-reports.zip trivy-reports

      - name: Generate upgrade suggestions JSON
        run: |
          set -euo pipefail
          jq '[.Results[].Vulnerabilities[]? 
               | select(.Severity=="HIGH" or .Severity=="CRITICAL") 
               | {package: .PkgName, current_version: .InstalledVersion, suggested_fix: .FixedVersion, severity: .Severity, cve: .VulnerabilityID}]' \
               trivy-reports/trivy-report.json > trivy-reports/upgrade-suggestions.json
          echo "📄 Arquivo de sugestões gerado em trivy-reports/upgrade-suggestions.json"

      - name: Show Trivy JSON and fail if needed
        if: ${{ env.TRIVY_FAIL_ON == 'true' }}
        run: |
          set -euo pipefail
          COUNT=$(jq '[.Results[].Vulnerabilities[]? | select(.Severity=="HIGH" or .Severity=="CRITICAL")] | length' trivy-reports/trivy-report.json)
          echo "📄 Relatório completo de vulnerabilidades:"
          jq '.' trivy-reports/trivy-report.json || echo "✅ Nenhuma vulnerabilidade crítica"
          if [ "$COUNT" -gt 0 ]; then
            echo "❌ Encontradas $COUNT vulnerabilidades HIGH/CRITICAL"
            exit 1
          else
            echo "✅ Nenhuma vulnerabilidade HIGH/CRITICAL encontrada"
          fi

      - name: Upload Trivy reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-reports
          path: trivy-reports/

      - name: Upload Trivy reports ZIP
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-reports-zip
          path: trivy-reports.zip

      - name: Install jq and yq
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y jq zip
          curl -sL https://github.com/mikefarah/yq/releases/download/v4.45.1/yq_linux_amd64 -o yq
          chmod +x yq
          sudo mv yq /usr/local/bin/

      - name: Update Fleet repo manifests
        run: |
          set -euo pipefail
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          REPO_NO_PROTO=${{ secrets.FLEET_REPO_URL#https:// }}
          CLONE_URL="https://${{ secrets.FLEET_REPO_PAT }}@${REPO_NO_PROTO}"
          BRANCH="${{ secrets.FLEET_BRANCH:-main }}"
          git clone --depth 1 --branch "${BRANCH}" "${CLONE_URL}" fleet-repo
          cd fleet-repo || exit 1

          MANIFEST="k8s/apps/${APP_NAME}/deployment.yaml"
          IMAGE="gru.ocir.io/${{ secrets.IMAGE_NAMESPACE }}/poc/image/${APP_NAME}:${SHORT_SHA}"
          PORT=${APP_PORT}

          yq e -i '.spec.template.spec.containers[0].image = strenv(IMAGE)' "$MANIFEST"
          yq e -i '.spec.template.spec.containers[0].ports[0].containerPort = strenv(PORT)' "$MANIFEST"

          if git diff --quiet --exit-code; then
            echo "✅ Nenhuma atualização necessária"
          else
            git add "$MANIFEST"
            git commit -m "Atualiza imagem e porta para ${APP_NAME}"
            git push origin "${BRANCH}"
            echo "🚀 Manifest atualizado no Fleet repo"
          fi
